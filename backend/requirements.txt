fastapi==0.104.1
uvicorn[standard]==0.24.0
num2words==0.5.13
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
asyncpg==0.29.0
# H5: was 0.0.6 — CVE-2024-24762 (ReDoS/DoS in multipart form parsing).
python-multipart==0.0.18
# H5 (follow-up, needs full regression in your CI): fastapi==0.104.1 + the
# Starlette it pins are affected by CVE-2024-47874 (multipart memory DoS).
# Upgrade to fastapi>=0.115 (Starlette>=0.40) and re-run the test suite —
# not auto-bumped here to avoid an unverified framework jump in production.
PyJWT[crypto]==2.10.1
bcrypt==5.0.0
python-decouple==3.8
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.3.0
aiosqlite==0.19.0
pandas==2.1.4
numpy==1.26.2
httpx==0.27.0
xhtml2pdf==0.2.15
redis==7.3.0
# Outgoing v2 render pipeline (HTML->DOCX, layout parsing). These were
# used in code but unpinned, so fresh envs / the test_portal venv came up
# without them and v2 render + auth (bs4) silently broke.
python-docx==1.1.2
beautifulsoup4==4.14.3
lxml==6.0.2
# L3: defused XML parsing for uploaded DOCX (entity-expansion DoS).
defusedxml==0.7.1
